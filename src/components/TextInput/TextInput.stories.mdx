import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import TextInput from "./TextInput";

<Meta
  title="Components/TextInput"
  component={TextInput}
  argTypes={{
    id: {
      description: "Handling the id of the input tag.",
      type: { required: false },
      defaultValue: "textinput",
    },
    width: {
      description:
        "Handling the width of the TextInput. ('px' and '%' units are ok.)",
      type: { required: false },
      defaultValue: "",
    },
    type: {
      description: "Handling the type of the TextInput.",
      type: { name: "string", required: false },
      defaultValue: "text",
    },
    defaultValue: {
      name: "defaultValue",
      type: { name: "string", required: false },
      description: "Specify the defaultValue of the TextInput.",
      defaultValue: "",
    },
    maxLength: {
      description: "Handling the maxLength of the TextInput.",
      type: { name: "number", required: false },
      defaultValue: "",
    },
    disabled: {
      name: "disabled",
      type: { name: "boolean", required: false },
      description: "handling the disabled state of the TextInput.",
      defaultValue: false,
    },
    placeholder: {
      description: "Specify the placeholder of the TextInput.",
      type: { name: "string", required: false },
      defaultValue: "placeholder",
    },
    label: {
      name: "label",
      type: { name: "string", required: false },
      description: "Specify the label of the TextInput.",
      defaultValue: "TextInput label",
    },
    hideLabel: {
      name: "hideLabel",
      type: { name: "boolean", required: false },
      description: "Controlling the displaying label.",
      defaultValue: false,
    },
    warn: {
      name: "warn",
      type: { name: "string", required: false },
      description: "Specify the warning of the TextInput.",
      defaultValue: "This is some helper text",
    },
    hideWarn: {
      name: "hideWarn",
      type: { name: "boolean", required: false },
      description: "Controlling the displaying warning.",
      defaultValue: false,
    },
    onChange: {
      name: "onChange",
      type: { required: false },
      description:
        "onChange is a utility for this controlled component to communicate to a consuming component what kind of internal state changes are occurring.",
      table: {
        type: {
          summary: "func",
        },
      },
    },
  }}
/>

# TextInput

## Overview

export const Template = (args) => <TextInput {...args} />;

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
