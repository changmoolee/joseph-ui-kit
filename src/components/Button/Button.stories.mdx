import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import Button from "./Button";

<Meta
  title="Components/Button"
  component={Button}
  argTypes={{
    kind: {
      name: "kind",
      type: { name: "select", required: false },
      description: "Handliing the kind of the Button.",
      defaultValue: "default",
      options: ["default", "secondary", "tertiary", "ghost", "danger"],
    },
    type: {
      name: "type",
      type: { name: "select", required: false },
      description: "Handliing the type of the button tag.",
      defaultValue: "button",
      options: ["button", "reset", "submit"],
    },
    name: {
      name: "name",
      type: { name: "string", required: false },
      description: "Specify the name of the Button.",
      defaultValue: "Button",
    },
    width: {
      name: "width",
      type: { name: "string", required: false },
      description: "Handliing the width of the Button.",
      defaultValue: "auto",
    },
    position: {
      name: "position",
      type: { name: "select", required: false },
      description: "Handliing the position of the text in the Button.",
      defaultValue: "left",
      options: ["left", "center", "right"],
    },
    padding: {
      name: "padding",
      type: { name: "string", required: false },
      description: "Handliing the padding of the Button.",
      defaultValue: "3px 60px 3px 12px",
    },
    size: {
      name: "size",
      type: { name: "select", required: false },
      description: "Specify the size of the Button.",
      table: {
        type: {
          summary: `small middle large xlarge 2xlarge`,
        },
      },
      defaultValue: "middle",
      options: ["small", "middle", "large", "xlarge", "2xlarge"],
    },
    border: {
      name: "border",
      type: { required: false },
      defaultValue: "none",
      control: { type: null },
      table: {
        disable: true,
      },
    },
    bgColor: {
      table: {
        disable: true,
      },
    },
    color: {
      type: { required: false },
      defaultValue: "white",
      control: { type: null },
      table: {
        disable: true,
      },
    },
    tabIndex: {
      name: "tabIndex",
      type: { name: "number", required: false },
      description: "Handling the tabindex property of the Button",
      defaultValue: 0,
    },
    hover: {
      table: {
        disable: true,
      },
    },
    focus: {
      table: {
        disable: true,
      },
    },
    active: {
      table: {
        disable: true,
      },
    },
    onClick: {
      name: "onClick",
      type: { required: false },
      description:
        "Provide an optional function to be called when the button element is clicked",
      table: {
        type: {
          summary: "func",
        },
      },
    },
    onMouseEnter: {
      name: "onMouseEnter",
      type: { required: false },
      description:
        "Provide an optional function to be called when the mouse enters the button element",
      table: {
        type: {
          summary: "func",
        },
      },
    },
    onMouseLeave: {
      name: "onMouseLeave",
      type: { required: false },
      description:
        "Provide an optional function to be called when the mouse leaves the button element",
      table: {
        type: {
          summary: "func",
        },
      },
    },
  }}
  parameters={{
    componentSubtitle: "Button",
  }}
/>

# Button

## Overview

Buttons are clickable elements that are used to trigger actions. They communicate calls to action to the user and allow users to interact with pages in a variety of ways. Button labels express what action will occur when the user interacts with it.

export const Template = (args) => <Button {...args} />;

<Canvas>
  <Story
    name="Default"
    args={{
      kind: "default",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Component Api

<ArgsTable story="Default" />

<Canvas>
  <Story
    name="Danger"
    args={{
      kind: "danger",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Ghost"
    args={{
      kind: "ghost",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Secondary"
    args={{
      kind: "secondary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Tertiary"
    args={{
      kind: "tertiary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
