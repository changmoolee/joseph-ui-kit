import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import FileUploaderDropContainer from "./FileUploaderDropContainer";

<Meta
  title="Components/FileUploaderDropContainer"
  component={FileUploaderDropContainer}
  argTypes={{
    width: {
      name: "width",
      type: { name: "string", required: false },
      description:
        "You can handle the width property of FileUploaderDropContainer.",
      table: {
        type: { summary: "string" },
      },
      defaultValue: "320px",
    },
    labelText: {
      name: "labelText",
      type: { name: "string", required: false },
      description: "Specify the label of the FileUploaderDropContainer.",
      table: {
        type: { summary: "string" },
      },
    },
    fileSize: {
      name: "fileSize",
      type: { name: "number", required: false },
      description:
        "You can handle the size of the image file you want to upload. (The standard unit is 'megabytes')",
      table: {
        type: { summary: "number" },
      },
      defaultValue: 0.5,
    },
    onChange: {
      name: "onChange",
      type: { required: false },
      description:
        "onChange is a utility for this controlled component to communicate to a consuming component what kind of internal state changes are occurring.",
      table: {
        type: {
          summary: "func",
        },
      },
    },
  }}
/>

# FileUploader

## Overview

File uploaders allow users to a file to upload to a specific location.

export const Template = (args) => <FileUploaderDropContainer {...args} />;

<Canvas>
  <Story name="Default" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Component Api

<ArgsTable story="Default" />
