import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import Tabs from "./Tabs";
import { TabList, Tab, TabPanels, TabPanel } from "./Tabs";

<Meta
  title="Components/Tabs"
  component={Tabs}
  argTypes={{
    width: {
      description:
        "Handling the width of the Tabs. ('px' and '%' units are ok.)",
      type: { required: false },
    },
    height: {
      description:
        "Handling the height of the Tabs. ('px' and '%' units are ok.)",
      type: { required: false },
    },
    children: {
      name: "children",
      description:
        "Provide child elements to be rendered inside of the Tabs. These elements should render either 'TabsList' or 'TabsDescription'.",
      table: {
        type: { summary: "node" },
      },
      control: {
        type: null,
      },
    },
    onClick: {
      name: "onClick",
      type: { required: false },
      description:
        "You can use the onClick property in only `<Tab>` element. Provide an optional function to be called when the `<Tab>` element is clicked",
      table: {
        type: {
          summary: "func",
        },
      },
      control: {
        type: null,
      },
    },
  }}
/>

# Tabs

## Overview

export const Template = (args) => <Tabs {...args} />;

<Canvas>
  <Story
    name="Default"
    args={{
      width: "600px",
      height: "50px",
      children: (
        <>
          <TabList>
            <Tab>child1</Tab>
            <Tab disabled>child2</Tab>
            <Tab>child3</Tab>
            <Tab>child4</Tab>
          </TabList>
          <TabPanels>
            <TabPanel>
              <div>child1</div>
            </TabPanel>
            <TabPanel>
              <div>child2</div>
            </TabPanel>
            <TabPanel>
              <div>child3</div>
            </TabPanel>
            <TabPanel>
              <div>child4</div>
            </TabPanel>
          </TabPanels>
        </>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Component Api

<ArgsTable story="Default" />
