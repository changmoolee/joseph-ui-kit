import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import Loading from "./Loading";

<Meta
  title="Components/Loading"
  component={Loading}
  argTypes={{
    size: {
      name: "size",
      type: { name: "select", required: false },
      description: "Loading 애니메이션의 사이즈를 설정합니다.",
      table: {
        type: {
          summary: `small middle large`,
        },
        defaultValue: { summary: "middle" },
      },
      defaultValue: "middle",
      options: ["small", "middle", "large"],
    },
    active: {
      name: "active",
      type: { name: "boolean", required: false },
      description: "Loading 애니메이션의 표시 여부를 설정합니다.",
      table: {
        type: { summary: "boolean" },
        defaultValue: { summary: true },
      },
      defaultValue: true,
    },
  }}
/>

# Loading

## Overview

Loading은 사용자에게 데이터 fetching 등의 활동이 완료될 때까지 기다리라고 알려주는 역할을 합니다.

export const Template = (args) => <Loading {...args} />;

<Canvas>
  <Story name="Default" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Component Api

<ArgsTable story="Default" />
